const degToRad = (d) => (d * Math.PI) / 180;

const radToDeg = (r) => (r * 180) / Math.PI;

var cones = 1; //para poder renderizar mais de um cone
var auxDeAnima = 0; //auxiliar para sempre começar a animaçao do zero